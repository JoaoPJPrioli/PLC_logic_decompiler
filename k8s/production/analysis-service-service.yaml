apiVersion: v1
kind: Service
metadata:
  name: analysis-service
  namespace: test-production
  labels:
    app: analysis-service
spec:
  selector:
    app: analysis-service
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
  type: ClusterIP
