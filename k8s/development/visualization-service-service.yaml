apiVersion: v1
kind: Service
metadata:
  name: visualization-service
  namespace: test-development
  labels:
    app: visualization-service
spec:
  selector:
    app: visualization-service
  ports:
  - port: 8084
    targetPort: 8084
    protocol: TCP
  type: ClusterIP
