apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: test-development
  labels:
    app: ai-service
spec:
  selector:
    app: ai-service
  ports:
  - port: 8083
    targetPort: 8083
    protocol: TCP
  type: ClusterIP
