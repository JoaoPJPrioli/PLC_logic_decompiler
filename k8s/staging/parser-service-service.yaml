apiVersion: v1
kind: Service
metadata:
  name: parser-service
  namespace: test-staging
  labels:
    app: parser-service
spec:
  selector:
    app: parser-service
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
