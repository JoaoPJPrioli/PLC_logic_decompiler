apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: test-staging
  labels:
    app: database
spec:
  selector:
    app: database
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP
